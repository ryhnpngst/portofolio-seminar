---
interface Props {
  class?: string;
  as?: string;
}

const { class: className = "", as: Tag = "div" } = Astro.props;
---

<Tag class={`max-w-[1200px] mx-auto px-5 sm:px-6 lg:px-8 ${className}`}>
  <slot />
</Tag>

